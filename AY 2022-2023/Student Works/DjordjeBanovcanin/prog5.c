#include <stdio.h>
#include <string.h>
/*
int main(){

	char sWord[12];
	int dVowels = 0, dLen = 0;

	printf("\n Hello, pls gib a word: ");
	scanf("%s", sWord);

	dLen = strlen(sWord);
	for (int i = 0; i < dLen; i++){
		switch(sWord[i]){
		case 'a':
		case 'e':
		case 'i':
		case 'o':
		case 'u': 
			dVowels++;
			sWord[i] = 'X';
			break;
		case '\0': break;
		default: break;
		}
	}

	printf("\n Your input has %d vowels", dVowels);
	printf("\n Your new word is now %s \n", sWord);
	return 0;
}
**********************

int main(){

	char str[20];
	int count = 0;
	scanf("%s", str);
	int len = strlen(str);

	for(int i = 0; i < len-1; i++){
		if(str[i] == 'u' && str[i+1] == 'w' && str[i+2] == 'u'){
			count++;
		}
	}
	printf("\n %d \n", count);

	return 0;
}
**********************
*/



int main(){

	



	return 0;
}