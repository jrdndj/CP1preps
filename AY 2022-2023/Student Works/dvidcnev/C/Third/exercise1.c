#include <stdio.h>

int main() {
	
	int dFactor=1;

	for ( ; dFactor<=100; dFactor++ ) {
		printf("\n \t%d \t x \t %d \t= \t%d \n", dFactor, dFactor, dFactor*dFactor); //joce
	}

	return 0;

}

//PRINT FACTORIZED NUMBERS TILL GIVEN LIMIT