#include<stdio.h>

int main(){

	int sInput;
	int count=0;
	int sum=0;

	printf("\nPlease type in the integersyou want\n");	
	printf("\nType 0 when you want the program to stop\n");
	int array[count];

	while(scanf("%d", &sInput)){
		if(sInput<0){
			printf("\nPlease enter another number\n");
		}
		else if(sInput==0){
			break;
		}
		else{
		int sum+=sInput;
		count++;
	}
	
	double average=(double)sum/count;
	




return 0;
}