#include<stdio.h>


int main(){
	
	char sPhrase[100];
	int count;
	
	puts("\n Please enter the substring\n");
	gets(sPhrase);

	for(int i=0; i<strlen(sPhrase); i++){
		if(sPhrase[i]==sPhrase[i+1]{
			count++;
			while(sPhrase[i]==sPhrase[i+1]
			i++
		}
	}
	printf("\n the number of the longest substrings is %d\n",count);

return 0;
}//end main