// This progam is written by Katarina Javor, 14.12.2020
// here we have a program that couns the number of substings without repeating characters
#include <stdio.h>
#include <string.h>
int main() {
    // declare varibles
    char sInput;
    int dLength=strlen(sInput);

    // interactive input (ask the user to enter string)
    printf("\n Insert a string: ");
    scanf("%s", sInput);
    
    // use for loop to go trough a sting
    for(int dIndex=0; dIndex<dLength; dIndex++) {
        
    }

    return 0;
} // end main