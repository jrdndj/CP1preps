#include <stdio.h>
#include <stdlib.h>

int main()
{
        //declaring variales
        int n;
        int t;
        int result = 1;
		int i = 1;
        
        //getting input
		printf("Enter how many numbers will you enter?");
		scanf("%d",&t);
		int array[t];


		printf("Enter the numbers:\n");
		for(int j=0; j<t; j++){
            scanf("%d",&array[j]);
		}

for(int k=0; k<t; k++){

int n2 = array[k]*array[k];



		result = result*i;

		while(result % n2 != 0 ){

			i++;

			result = result*i;

		}//endwhile
		printf("\n%d",i);
}//endfor
    return 0;
}